import React from 'react'

export interface {{ pascalCase name }}Props extends React.ComponentPropsWithRef<'{{{ lowerCase tag }}}'>{}

export const {{ pascalCase name }}: React.FC<{{ pascalCase name }}Props> = React.forwardRef((props, ref) => {
  return (<{{{ lowerCase tag }}} ref={ref}></{{{ lowerCase tag }}}>)
})