import React from 'react'
import { classNames } from '../../utils'

export const ModulesTimeline: React.FC<{className: string}> = ({ className }) => {
  return <div className={classNames(className)}></div>
}