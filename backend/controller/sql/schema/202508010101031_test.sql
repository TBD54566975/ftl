-- migrate:up

CREATE TABLE testing_do_not_merge
(
    id         BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    created_at TIMESTAMPTZ NOT NULL DEFAULT (NOW() AT TIME ZONE 'utc'),
    module     TEXT,  -- If NULL, configuration is global.
    name       TEXT   NOT NULL,
    url        TEXT   NOT NULL,
    UNIQUE (module, name)
);

-- migrate:down
