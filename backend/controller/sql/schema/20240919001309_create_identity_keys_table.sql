-- migrate:up

CREATE TABLE identity_keys (
    id          BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    key         bytea NOT NULL,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT (NOW() AT TIME ZONE 'utc'),
    verify_signature TEXT
);

-- migrate:down
