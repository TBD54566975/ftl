#!/bin/bash
set -euo pipefail
ftldir="$(dirname "$0")/.."
dest="${TMPDIR:-/tmp}/ftl"
mkdir -p "$dest"
(cd "${ftldir}" && ./bin/go build -trimpath -buildvcs=false -ldflags="-s -w -buildid=" -o "$dest/ftl" ./cmd/ftl) && exec "$dest/ftl" "$@"
